# Machine Translation
After training from `translation_data` datasets, the model can translate simple Swedish sentences to English sentences. LSTM and Encoder-Decoder architecture are implemented in this translation model. BLEU metric is used to automaticaly evaluate the quality of translations.
